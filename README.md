# LucyEatReminder

---
## Custom Texture Pack
To change the image shown, use the [Example Pack](https://github.com/TastyPommesLul/LucyEatReminder/tree/master/example-texturepack) and replace the image with a custom one (128x128 in size), <br>with the same name as the already provided one.